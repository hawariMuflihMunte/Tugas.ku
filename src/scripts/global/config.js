const CONFIG = {
  /*
    Please consider the implications of changing this variable
    as it is integrated with current data storage system.
    Any modifications to the key will result in the creation
    of a new data storage instance, leaving the previous data
    storage unused and consuming computer memory without being
    properly deleted.
  */
  'APP_LOCAL_STORAGE_KEY': 'hHNU4rfAgCzvTWHQxNGA'
};

export default CONFIG;
